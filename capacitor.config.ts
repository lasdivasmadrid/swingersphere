import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lasdivasmadrid.swingersphere',
  appName: 'SwingerSphere',
  webDir: 'www'
};

export default config;
